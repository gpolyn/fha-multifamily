# describe "TodoApp.template helper function", ->
# 
# 	it "should result in a template", ->
# 		loadFixtures("_item_template.html")
# 		template = Handlebars.compile($('#item-template').html())
# 		model = new TodoApp.Todo
# 		model.set(done: true, {silent:true})
# 		result = template model.toJSON()
# 		expect(result).toContain("<div class='todo done'>")
    
  